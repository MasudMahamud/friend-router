import React from 'react';
import './NotFound.css';

const NotFound = () => {
    return (
        <div className="no-match">
            <p>Page cannot be found !</p>
        </div>
    );
};

export default NotFound;